# Игра Сапёр (Minesweeper) для Консоли

Богатая функциями консольная игра Сапёр для Windows, написанная на языке C, с достижениями, статистикой, магазином, способностями и множеством тем оформления.

## Возможности

- **Несколько Уровней Сложности**: Лёгкий, Средний, Сложный и Пользовательский
- **Система Достижений**: 15 различных достижений для разблокировки
- **Отслеживание Статистики**: Отслеживайте свои победы, поражения, лучшее время и серии побед
- **Система Магазина**: Зарабатывайте монеты и покупайте способности (Детектор, Щит, Сканер)
- **Способности**: Используйте специальные способности, чтобы помочь себе выиграть
- **Множество Тем**: Выбирайте из различных визуальных тем
- **Настройки**: Настройте свой игровой опыт, включая режим Бога
- **Модификаторы**: Опциональные игровые модификаторы для дополнительного вызова

---

## 📋 ПОЛНАЯ ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ

### 🪟 ДЛЯ ПОЛЬЗОВАТЕЛЕЙ WINDOWS

#### Шаг 1: Установка Компилятора (MinGW-w64)

**Вариант А: Установка через MSYS2 (Рекомендуется - Самый Простой)**

1. **Скачайте MSYS2:**
   - Перейдите на сайт: https://www.msys2.org/
   - Нажмите кнопку "Download Installer" (Скачать установщик)
   - Скачайте файл `msys2-x86_64-*.exe` (около 100 МБ)

2. **Установите MSYS2:**
   - Запустите скачанный установщик
   - Следуйте инструкциям установщика (нажимайте "Далее")
   - Оставьте путь установки по умолчанию: `C:\msys64`
   - Дождитесь завершения установки

3. **Установите MinGW через MSYS2:**
   - Откройте "MSYS2 MSYS" из меню Пуск
   - В открывшемся окне введите следующую команду и нажмите Enter:
   ```bash
   pacman -Syu
   ```
   - Дождитесь завершения обновления (может занять несколько минут)
   - Когда появится запрос, нажмите Enter для закрытия окна
   - Снова откройте "MSYS2 MSYS" и введите:
   ```bash
   pacman -S --needed base-devel mingw-w64-x86_64-gcc
   ```
   - Нажмите Enter и дождитесь установки (может занять 5-10 минут)
   - Когда появится запрос `[Y/n]`, введите `Y` и нажмите Enter

4. **Добавьте MinGW в системную переменную PATH:**
   - Нажмите `Win + R` (клавиша Windows + R)
   - Введите: `sysdm.cpl` и нажмите Enter
   - Перейдите на вкладку "Дополнительно"
   - Нажмите кнопку "Переменные среды"
   - В разделе "Системные переменные" найдите переменную `Path` и нажмите "Изменить"
   - Нажмите "Создать" и добавьте: `C:\msys64\mingw64\bin`
   - Нажмите "ОК" во всех открытых окнах
   - **ВАЖНО:** Закройте все окна командной строки и откройте их заново

5. **Проверьте установку:**
   - Откройте Командную строку (Win + R, введите `cmd`, нажмите Enter)
   - Введите команду:
   ```cmd
   gcc --version
   ```
   - Если вы видите версию GCC (например, `gcc.exe (Rev1, Built by MSYS2 project) 13.2.0`), значит всё установлено правильно!
   - Если видите ошибку "gcc не является внутренней или внешней командой", вернитесь к шагу 4

**Вариант Б: Прямая установка MinGW-w64 (Альтернативный способ)**

1. Перейдите на: https://www.mingw-w64.org/downloads/
2. Выберите "MingW-W64-builds"
3. Скачайте установщик
4. Запустите установщик и следуйте инструкциям
5. При установке выберите:
   - Version: последняя версия
   - Architecture: x86_64
   - Threads: posix
   - Exception: seh
6. Укажите путь установки (например, `C:\mingw64`)
7. Добавьте путь `C:\mingw64\bin` в системную переменную PATH (как в шаге 4 Варианта А)

#### Шаг 2: Скачивание Исходного Кода Игры

1. **Скачайте все файлы игры:**
   - Убедитесь, что у вас есть все следующие файлы в одной папке:
     - `main.c`
     - `common.c`, `common.h`
     - `display.c`, `display.h`
     - `input.c`, `input.h`
     - `menu.c`, `menu.h`
     - `game.c`, `game.h`
     - `settings.c`, `settings.h`
     - `store.c`, `store.h`
     - `statistics.c`, `statistics.h`
     - `achievements.c`, `achievements.h`
     - `abilities.c`, `abilities.h`
     - `theme.c`, `theme.h`
     - `compile.bat`
     - `Makefile`

2. **Создайте папку для игры** (если её ещё нет):
   - Например: `C:\Games\minesweeper` или `D:\coding\minesweeper`
   - Скопируйте все файлы в эту папку

#### Шаг 3: Компиляция Игры (Сборка)

**Способ 1: Использование Batch-файла (Самый Простой - Рекомендуется)**

1. **Откройте Командную строку:**
   - Нажмите `Win + R`
   - Введите `cmd` и нажмите Enter
   - Или найдите "Командная строка" в меню Пуск

2. **Перейдите в папку с игрой:**
   ```cmd
   cd C:\Games\minesweeper
   ```
   *(Замените `C:\Games\minesweeper` на путь к вашей папке с игрой)*
   
   **Совет:** Чтобы узнать путь к папке:
   - Откройте папку с игрой в Проводнике
   - Кликните в адресной строке (где показан путь)
   - Нажмите `Ctrl + C` чтобы скопировать путь
   - В командной строке введите `cd ` (с пробелом) и нажмите `Ctrl + V`, затем Enter

3. **Запустите компиляцию:**
   ```cmd
   compile.bat
   ```
   - Нажмите Enter
   - Дождитесь завершения компиляции (обычно 5-10 секунд)

4. **Проверьте результат:**
   - Если вы видите сообщение "Compilation successful!" - всё готово!
   - В папке должен появиться файл `minesweeper.exe`
   - Если видите ошибки, перейдите в раздел "Решение Проблем" ниже

**Способ 2: Использование Makefile**

1. Откройте Командную строку в папке с игрой (как в Способе 1, шаг 1-2)

2. Выполните команду:
   ```cmd
   make
   ```
   - Если команда `make` не найдена, установите Make:
     - Скачайте с: https://sourceforge.net/projects/gnuwin32/files/make/
     - Или используйте Способ 1 (compile.bat)

3. После успешной компиляции запустите игру:
   ```cmd
   minesweeper.exe
   ```

**Способ 3: Ручная Компиляция (Для Продвинутых)**

1. Откройте Командную строку в папке с игрой

2. Выполните команду компиляции:
   ```cmd
   gcc main.c common.c display.c input.c menu.c game.c settings.c store.c statistics.c achievements.c abilities.c theme.c -o minesweeper.exe -Wall -Wextra -std=c99 -O2
   ```

3. Если компиляция прошла успешно (нет ошибок), запустите:
   ```cmd
   minesweeper.exe
   ```

#### Шаг 4: Запуск Игры

**Вариант 1: Через Командную строку**
1. Откройте Командную строку в папке с игрой
2. Введите команду:
   ```cmd
   minesweeper.exe
   ```
3. Нажмите Enter

**Вариант 2: Двойной клик**
1. Откройте папку с игрой в Проводнике
2. Найдите файл `minesweeper.exe`
3. Дважды кликните по нему

**Вариант 3: Создание ярлыка**
1. Правой кнопкой мыши на `minesweeper.exe`
2. Выберите "Создать ярлык"
3. Переместите ярлык на Рабочий стол
4. Запускайте игру двойным кликом по ярлыку

---

### 🐧 ДЛЯ ПОЛЬЗОВАТЕЛЕЙ LINUX

**⚠️ ВАЖНОЕ ПРИМЕЧАНИЕ:** Эта игра использует Windows-специфичные API (`windows.h`). Для запуска на Linux есть несколько вариантов:

#### Вариант 1: Использование Wine (Рекомендуется)

1. **Установите Wine:**
   
   **Для Ubuntu/Debian:**
   ```bash
   sudo apt update
   sudo apt install wine
   ```
   
   **Для Fedora:**
   ```bash
   sudo dnf install wine
   ```
   
   **Для Arch Linux:**
   ```bash
   sudo pacman -S wine
   ```

2. **Скомпилируйте игру на Windows** (следуйте инструкциям для Windows выше) или получите готовый `minesweeper.exe`

3. **Скопируйте `minesweeper.exe` на Linux-машину**

4. **Запустите игру через Wine:**
   ```bash
   wine minesweeper.exe
   ```
   
   При первом запуске Wine может попросить установить дополнительные компоненты - следуйте инструкциям на экране.

#### Вариант 2: Использование WSL (Windows Subsystem for Linux)

Если вы используете Windows 10/11, вы можете установить WSL и использовать Wine внутри него:

1. **Установите WSL2:**
   ```powershell
   wsl --install
   ```
   (Выполните в PowerShell от имени администратора)

2. **Установите Wine в WSL:**
   ```bash
   sudo apt update
   sudo apt install wine
   ```

3. **Скомпилируйте игру на Windows** и скопируйте `minesweeper.exe` в WSL

4. **Запустите через Wine:**
   ```bash
   wine minesweeper.exe
   ```

#### Вариант 3: Портирование Кода (Только для Продвинутых)

Этот вариант требует модификации исходного кода для замены Windows API на Linux-эквиваленты (например, ncurses). Это сложная задача, требующая знаний программирования.

---

## 🎮 Управление в Игре

### Навигация по Главному Меню
- **Стрелки Вверх/Вниз** или **W/S**: Перемещение по пунктам меню
- **Enter** или **Пробел**: Выбрать пункт меню
- **ESC**: Выход из игры

### Управление в Игровом Процессе
- **Стрелки** или **WASD**: Перемещение курсора по полю
- **Пробел** или **Enter**: Открыть ячейку
- **F**: Установить/убрать флаг на ячейке
- **D**: Использовать способность Детектор (если куплена в магазине)
- **S**: Использовать способность Щит (если куплена в магазине)
- **C**: Использовать способность Сканер (если куплена в магазине)
- **ESC**: Вернуться в главное меню

---

## 📁 Файлы Игры

Игра автоматически создаёт следующие файлы для сохранения вашего прогресса:

- `minesweeper_stats.dat` - Ваша игровая статистика (победы, поражения, лучшее время)
- `minesweeper_achievements.dat` - Ваши разблокированные достижения
- `minesweeper_settings.dat` - Ваши настройки игры (тема, режим Бога и т.д.)
- `minesweeper_store.dat` - Ваши покупки в магазине и количество монет

**⚠️ ВАЖНО:** Эти файлы сохраняют весь ваш прогресс! Не удаляйте их, иначе потеряете:
- Все достижения
- Статистику игр
- Купленные способности
- Накопленные монеты

---

## 🔧 Решение Проблем

### Проблемы Windows

#### Ошибка: "gcc не является внутренней или внешней командой"

**Причина:** GCC не установлен или не добавлен в PATH

**Решение:**
1. Проверьте, установлен ли MinGW:
   ```cmd
   where gcc
   ```
   Если команда ничего не выводит, MinGW не установлен или не в PATH

2. Убедитесь, что вы добавили путь к MinGW в системную переменную PATH:
   - Для MSYS2: `C:\msys64\mingw64\bin`
   - Для прямой установки: `C:\mingw64\bin` (или ваш путь установки)

3. **ВАЖНО:** После изменения PATH:
   - Закройте ВСЕ окна командной строки
   - Откройте новое окно командной строки
   - Попробуйте снова: `gcc --version`

4. Если проблема остаётся, переустановите MinGW (см. Шаг 1 инструкции выше)

#### Ошибка: "Compilation failed" или "Ошибка компиляции"

**Причина:** Отсутствуют файлы или ошибки в коде

**Решение:**
1. Убедитесь, что ВСЕ файлы находятся в одной папке:
   - Все `.c` файлы (main.c, common.c, display.c и т.д.)
   - Все `.h` файлы (common.h, display.h и т.д.)
   - Файл `compile.bat`

2. Проверьте, что у вас достаточно места на диске (нужно минимум 10 МБ свободного места)

3. Проверьте работу компилятора:
   ```cmd
   gcc --version
   ```
   Должна отобразиться версия GCC

4. Попробуйте скомпилировать вручную, чтобы увидеть конкретную ошибку:
   ```cmd
   gcc main.c common.c display.c input.c menu.c game.c settings.c store.c statistics.c achievements.c abilities.c theme.c -o minesweeper.exe
   ```

#### Ошибка: "Игра не запускается" или "minesweeper.exe не найден"

**Причина:** Файл не был создан или находится в другой папке

**Решение:**
1. Проверьте, что файл `minesweeper.exe` существует в папке с игрой
2. Убедитесь, что вы запускаете игру из правильной папки
3. Попробуйте указать полный путь:
   ```cmd
   C:\Games\minesweeper\minesweeper.exe
   ```
   (Замените на ваш путь)

4. Если файл не создался, проверьте сообщения об ошибках при компиляции

#### Ошибка: "Отказано в доступе" при запуске

**Причина:** Антивирус блокирует программу

**Решение:**
1. Добавьте папку с игрой в исключения антивируса
2. Или временно отключите антивирус для компиляции и запуска

### Проблемы Linux

#### Ошибка: "Windows API не найден" или "windows.h: No such file or directory"

**Причина:** Игра использует Windows-специфичные функции

**Решение:**
- Используйте Wine для запуска скомпилированного `.exe` файла (см. инструкции выше)
- Или портируйте код на Linux (требует знаний программирования)

#### Ошибка: "Makefile не работает" или "make: command not found"

**Решение:**
1. Установите make:
   ```bash
   sudo apt install make  # Ubuntu/Debian
   sudo dnf install make  # Fedora
   sudo pacman -S make    # Arch
   ```

2. Учтите, что Makefile может не работать без портирования кода на Linux

---

## 💻 Системные Требования

- **Операционная Система:**
  - Windows 7, 8, 10, 11 (нативная поддержка)
  - Linux с Wine (совместимость через эмуляцию)
  
- **ОЗУ:** Минимальное (консольное приложение, менее 50 МБ)

- **Место на Диске:** 
  - Исходный код: ~100 КБ
  - Исполняемый файл: менее 1 МБ
  - Всего: менее 2 МБ

- **Терминал:**
  - Windows: Командная строка (cmd) или PowerShell
  - Linux: Терминал (bash, zsh и т.д.)

---

## 📝 Краткая Шпаргалка Команд

### Windows (Командная строка)

```cmd
REM Перейти в папку с игрой
cd C:\Games\minesweeper

REM Проверить установку GCC
gcc --version

REM Скомпилировать игру
compile.bat

REM Запустить игру
minesweeper.exe
```

### Linux (Терминал)

```bash
# Установить Wine (Ubuntu/Debian)
sudo apt install wine

# Запустить игру через Wine
wine minesweeper.exe
```

---

## 📞 Поддержка

Если вы столкнулись с проблемами, которые не описаны выше:

1. **Проверьте наличие всех файлов:**
   - Убедитесь, что все `.c` и `.h` файлы присутствуют
   - Проверьте, что `compile.bat` находится в той же папке

2. **Проверьте установку компилятора:**
   - Выполните `gcc --version` в командной строке
   - Если команда не работает, переустановите MinGW

3. **Проверьте права доступа:**
   - Убедитесь, что у вас есть права на запись в папку с игрой
   - Попробуйте запустить командную строку от имени администратора

4. **Проверьте антивирус:**
   - Некоторые антивирусы могут блокировать компиляцию
   - Добавьте папку в исключения

---

## 📄 Лицензия

Эта игра предоставляется "как есть" в образовательных и развлекательных целях.

---

**Приятной игры в Сапёр! 🎮💣**
